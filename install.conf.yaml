---
- defaults:
    link:
      relink: true
      force: true
      create: true
    create:
      mode: 0755

- create: 
  - ~/bin
  - ~/source

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.zshrc:
    ~/.fzf.zsh:
    ~/.oh-my-zsh:
    ~/.p10k.zsh:
