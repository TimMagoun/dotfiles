- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.ssh/config:
       path: ssh/config
       create: true
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.config/kitty/kitty.conf: kitty.conf
